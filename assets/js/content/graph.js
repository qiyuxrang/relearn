/* ============================================================
   图论模块课程内容（graph-basic ~ graph-flow，共 10 节）
   纯数据文件：只导出 lessons，不 import 任何东西
   动画 kind 以字符串引用，见 SPEC「可用动画 kind 列表」
   ============================================================ */

export const lessons = {
  /* ------------------------------------------------------------
     1. 图的表示：邻接矩阵与邻接表
     ------------------------------------------------------------ */
  'graph-basic': {
    lede: '图是「关系」的数学模型：地图、依赖、社交网络本质上都是图。这一节把术语一次说清，再看两种存图方式怎么选、C++ 里怎么写。',
    blocks: [
      { t: 'h2', x: '为什么要学图' },
      { t: 'p', x: '数组处理线性关系，树处理层级关系，但现实里更多的是**网状关系**：城市之间有公路，课程之间有先修要求，网页之间有链接。图（Graph）就是这类关系的统一模型——一堆**顶点**（vertex）加一堆**边**（edge）。图论算法再花哨，也都建立在两件小事上：把图存进内存、沿着边走。本节先解决第一件。' },
      { t: 'h2', x: '先把术语说清楚' },
      { t: 'ul', x: [
        '**无向图 / 有向图**：边有没有方向。微信好友是无向的（互为好友），微博关注是有向的（你关注他，他未必关注你）。',
        '**权（weight）**：边上附带的数值，比如两城之间的距离、汇率、费用。没有权就叫无权图。',
        '**度（degree）**：一个点连了几条边。有向图分**入度**（指向我的边数）和**出度**（我指出去的边数）。',
        '**路径 / 环**：顺着边走出来的顶点序列叫路径；起点终点相同、长度大于零的路径叫环。',
        '**连通**：无向图里任意两点都有路可达就叫连通图；不连通的图由若干**连通分量**组成。',
      ] },
      { t: 'p', x: '一个常用的小事实：无向图所有点的度数之和等于边数的两倍（每条边给两端各贡献一个度）。做题时可以拿它验算建图有没有漏边。' },
      { t: 'h2', x: '两种存法：矩阵还是表' },
      { t: 'p', x: '主流方案就两种。**邻接矩阵**：开一个 V×V 的二维数组，`a[u][v]` 记录 u 到 v 有没有边（或边权是多少）。**邻接表**：给每个点开一个列表，只存它真正连着的邻居。下面的动画把同一张图的两种表示摆在一起，建议把每个顶点都点一遍，看两边分别怎么回答「它的邻居是谁」。' },
      { t: 'viz', kind: 'graph-repr' },
      { t: 'table',
        head: ['操作 / 指标', '邻接矩阵', '邻接表'],
        rows: [
          ['空间', '`O(V²)`', '`O(V+E)`'],
          ['查「u、v 之间有没有边」', '`O(1)`', '`O(deg(u))`'],
          ['遍历 u 的全部邻居', '`O(V)`', '`O(deg(u))`'],
          ['加一条边', '`O(1)`', '`O(1)`'],
          ['适合', '点少的稠密图、频繁查边', '稀疏图（绝大多数题目）'],
        ] },
      { t: 'p', x: '结论很直接：算法题里的图大多是**稀疏图**（E 远小于 V²），默认用邻接表。只有点数很小（几百以内）、又要频繁问「u 和 v 之间有没有边」时，矩阵才占便宜——Floyd 那节你会再见到它。' },
      { t: 'h2', x: 'C++ 三种建图写法' },
      { t: 'p', x: '同一张无向图，三种常见写法各建一遍：`vector<vector<int>>` 是无权图标配；`vector<pair<int,int>>` 给边带上权；**链式前向星**是竞赛圈的手写邻接表——用数组模拟链表，省内存、常数小。前两种必须熟练，第三种现在混个眼熟即可。' },
      { t: 'code', lang: 'cpp', name: 'build_graph.cpp（三种建图，可直接编译）', x: `#include <cstdio>
#include <vector>
using namespace std;

const int N = 5;                  // 顶点数（编号 0~4）
const int M = 20;                 // 边数上限（无向图记得开 2 倍）

// 方式一：邻接表（最常用）—— g[u] 存 u 的所有邻居
vector<vector<int>> g(N);

// 方式二：带权邻接表 —— 存 pair{邻居, 边权}
vector<vector<pair<int,int>>> gw(N);

// 方式三：链式前向星 —— 数组模拟的邻接表
int head[N], toE[M], nxtE[M], cnt = 0;
void addEdge(int u, int v) {
    toE[cnt] = v;                 // 这条边指向 v
    nxtE[cnt] = head[u];          // 接到 u 原来的第一条边前面
    head[u] = cnt++;              // u 的链表头指向新边
}

int main() {
    for (int i = 0; i < N; i++) head[i] = -1;

    // 建同一张无向图：0-1  0-2  1-3  2-3  3-4
    int es[5][2] = {{0,1},{0,2},{1,3},{2,3},{3,4}};
    for (auto& e : es) {
        int u = e[0], v = e[1];
        g[u].push_back(v);   g[v].push_back(u);        // 无向图两个方向都要加
        gw[u].push_back({v, 1}); gw[v].push_back({u, 1});
        addEdge(u, v);       addEdge(v, u);
    }

    printf("邻接表里 3 的邻居: ");
    for (int v : g[3]) printf("%d ", v);               // 1 2 4
    printf("\\n前向星里 3 的邻居: ");
    for (int e = head[3]; e != -1; e = nxtE[e])
        printf("%d ", toE[e]);                          // 4 2 1（按加边逆序）
    printf("\\n");
    return 0;
}` },
      { t: 'callout', kind: 'warn', title: '三个建图常见坑', x: [
        '**无向图要加两条边**：`addEdge(u,v)` 与 `addEdge(v,u)` 缺一不可。漏一半后 BFS/DFS 只能单向走，是图题 WA 榜第一名。',
        '**编号从 0 还是 1**：题目给 1~n 的点，数组就开 n+1、直接用原编号，别硬做减一映射——两种编号混用迟早串。',
        '**重边与自环**：题目没保证是简单图时，矩阵写 `a[u][v] = w` 会把重边覆盖掉（求最短路应取较小值），邻接表则天然容纳重边。',
      ] },
      { t: 'quiz',
        q: '一张 V = 10^5、E = 2×10^5 的稀疏图，用 int 邻接矩阵存，大约要多少内存？',
        opts: ['约 4 MB', '约 40 MB', '约 40 GB', '约 400 MB'],
        answer: 2,
        explain: '矩阵要开 V² = 10^10 个格子，每个 int 4 字节，约 40 GB，根本开不下。邻接表只需 O(V+E)，几 MB 就够。这就是「稀疏图必用邻接表」的原因——不是风格问题，是能不能活的问题。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: '图 = 顶点 + 边。稀疏图用邻接表（`vector<vector<int>>` 起步），点少的稠密图或需要 O(1) 查边才用矩阵；无向图一条边存两遍；度数和 = 2E 可用来验算。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '先用两道热身题找回「度」的手感，再用克隆图检验邻接表遍历是否真的过关。', x: [
        { id: 1791, title: '找出星型图的中心节点', slug: 'find-center-of-star-graph', level: '简单',
          why: '练「度」的概念：星型图的中心就是度数最大的那个点。',
          hint: '中心点连着其余所有点，所以它必然同时出现在任意两条边里——其实只看前两条边就能锁定它。',
          insight: '最优解只比较 `edges[0]` 和 `edges[1]` 的四个端点，公共端点即中心，O(1) 时间 O(1) 空间。统计全部度数再找最大值是 O(E)，也对但多余。坑：公共端点可能出现在两条边的任一位置，四种组合都要比到。' },
        { id: 997, title: '找到小镇的法官', slug: 'find-the-town-judge', level: '简单',
          why: '练有向图的入度 / 出度统计：法官 = 入度 n-1 且出度 0 的点。',
          hint: '把「a 信任 b」看成有向边 a→b。法官被所有其他人信任、却不信任任何人——开入度出度两个计数数组就够，不必真建邻接表。',
          insight: '一遍扫描统计入度出度，找 indeg == n-1 且 outdeg == 0 的点，O(N+E) 时间 O(N) 空间。技巧：可合并成一个分数 score[b]++、score[a]--，最后找 score == n-1。坑：n = 1 且 trust 为空时答案是 1，别漏这个边界。' },
        { id: 133, title: '克隆图', slug: 'clone-graph', level: '中等',
          why: '综合练邻接表结构 + 图遍历：深拷贝一张图，等于一边遍历一边建图。',
          hint: '难点在环：见点就复制会绕着环转圈。准备一张「原节点 → 新节点」的哈希表，进入节点先查表，克隆过就直接返回已有副本。',
          insight: ['DFS / BFS 均可：哈希表兼任 visited 标记，遇到未克隆的点先创建副本、再递归处理邻居，O(V+E) 时间 O(V) 空间。', '坑：必须在**递归邻居之前**就把副本放进哈希表，否则环会导致无限递归；空图（null 输入）要单独返回。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     2. BFS：一圈一圈往外扩
     ------------------------------------------------------------ */
  'graph-bfs': {
    lede: '广度优先搜索按「离起点多远」一圈圈向外扩。这个朴素的顺序有个漂亮的副产品：它就是无权图的最短路算法。',
    blocks: [
      { t: 'h2', x: '从水波纹说起' },
      { t: 'p', x: '往湖里丢一颗石子，水波一圈圈向外传：先到一步之内的地方，再到两步之内的。BFS 就是图上的水波纹——从起点出发，先访问距离为 1 的所有点，再访问距离为 2 的所有点。实现只需要一个**队列**：发现新点就排到队尾，处理时从队头取。先进先出，天然保证「近的先被处理」。下面的动画重点看队列的进出和分层染色。' },
      { t: 'viz', kind: 'graph-bfs' },
      { t: 'h2', x: '为什么 BFS 就是无权图最短路' },
      { t: 'p', x: '把「到起点距离恰好为 k 的点」叫作第 k 层，做个归纳：第 0 层只有起点，距离 0，显然正确。假设前 k 层的距离都算对了，那么第 k+1 层的任何点第一次被发现时，一定是被某个第 k 层的点扩展出来的——如果它其实有更短的路（距离 ≤ k），它早就在更早的层被发现了。于是结论成立：**每个点第一次入队时记下的层数，就是它的最短距离**。这个论证也顺手解释了前提：一旦边有长短之分，「层数」就不再等于「距离」，BFS 的结论立刻失效——那是下下节 Dijkstra 的事。' },
      { t: 'h2', x: '模板：网格迷宫最短路' },
      { t: 'p', x: 'BFS 出场率最高的舞台是网格图：每个格子是顶点，上下左右相邻是边。方向数组 `dx/dy` 加一层越界判断，是网格题的标准姿势。下面是一份能直接跑的迷宫最短路：' },
      { t: 'code', lang: 'cpp', name: 'maze_bfs.cpp（迷宫最短路，可直接编译）', x: `#include <cstdio>
#include <queue>
using namespace std;

int n = 5, m = 6;
const char* maze[5] = {        // '.' 可走，'#' 是墙
    "S.#...",
    ".##.#.",
    "......",
    ".#.##.",
    "...#.T"
};
int dist[5][6];
int dx[4] = {-1, 1, 0, 0}, dy[4] = {0, 0, -1, 1};

int main() {
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++) dist[i][j] = -1;    // -1 表示未访问

    queue<pair<int,int>> q;
    dist[0][0] = 0;                      // 起点 S 在 (0,0)
    q.push({0, 0});

    while (!q.empty()) {
        auto [x, y] = q.front(); q.pop();
        for (int d = 0; d < 4; d++) {
            int nx = x + dx[d], ny = y + dy[d];
            if (nx < 0 || nx >= n || ny < 0 || ny >= m) continue;   // 越界
            if (maze[nx][ny] == '#') continue;                      // 撞墙
            if (dist[nx][ny] != -1) continue;                       // 已访问
            dist[nx][ny] = dist[x][y] + 1;      // 入队那一刻就写距离
            q.push({nx, ny});
        }
    }
    printf("S 到 T 的最短步数: %d\\n", dist[4][5]);    // 输出 9
    return 0;
}` },
      { t: 'callout', kind: 'danger', title: '标记时机：入队时，不是出队时', x: '新点必须在**入队那一刻**就标记（本模板用 `dist != -1` 兼任访问标记），而不是等出队再标记。晚标记的后果：同一个点会被多个邻居重复塞进队列，网格题里队列膨胀好几倍，时间和内存一起爆。这是 BFS 最隐蔽也最常见的 bug，检查自己的模板时第一眼看这里。' },
      { t: 'h2', x: '两个常用变体' },
      { t: 'ul', x: [
        '**多源 BFS**：把多个起点在开局全部入队（距离都记 0），跑一次就得到「每个点到最近起点」的距离。典型题：腐烂的橘子、地图上每格离最近的 0 有多远。改动只有初始化那几行。',
        '**0-1 BFS**：边权只有 0 和 1 时不必上 Dijkstra——把队列换成双端队列：走 0 边到达的点不增加距离，插到**队头**（算同一层）；走 1 边的排到**队尾**（下一层）。整体仍是线性复杂度。',
      ] },
      { t: 'code', lang: 'cpp', name: '0-1 BFS 核心片段', x: `// 边权只有 0/1：用 deque 维持「距离单调」这一 BFS 命脉
deque<int> dq;
dist[s] = 0; dq.push_back(s);
while (!dq.empty()) {
    int u = dq.front(); dq.pop_front();
    for (auto [v, w] : g[u])              // w 只能是 0 或 1
        if (dist[u] + w < dist[v]) {
            dist[v] = dist[u] + w;
            if (w == 0) dq.push_front(v); // 0 边：同层，插队头
            else        dq.push_back(v);  // 1 边：下一层，排队尾
        }
}` },
      { t: 'quiz',
        q: '关于无权图上的搜索，下面哪个说法是对的？',
        opts: [
          'BFS 和 DFS 都能保证第一次到达某点走的是最短路',
          'BFS 按层扩展，第一次到达某点时的层数就是最短距离',
          '想求最短路必须用优先队列',
          'BFS 只能在连通图上使用',
        ],
        answer: 1,
        explain: 'DFS 一条路走到黑，第一次到达完全可能绕了远路；BFS 的「层」在无权图里天然等于距离，不需要堆——堆是边权不等时（Dijkstra）才请的外援。图不连通时 BFS 照样能跑，只是到不了的点距离保持 -1。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: 'BFS = 队列 + 入队即标记；无权图最短路 = 第一次入队时的层数；多起点就全部初始入队；边权只有 0/1 用双端队列。命脉是队列里的距离**单调不减**——记住这句，后面理解 Dijkstra 就是换容器。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '三道题对应本节三个层次：裸网格 BFS、多源 BFS、把隐式图抽象出来再 BFS。', x: [
        { id: 733, title: '图像渲染', slug: 'flood-fill', level: '简单',
          why: '最裸的网格 BFS（洪泛填充）：把方向数组 + 越界判断练成肌肉记忆。',
          hint: '先记下起点的原始颜色，再把与起点四连通、颜色相同的格子全部改成新颜色。BFS、DFS 都行。',
          insight: '标准洪泛填充，O(M×N) 时间，队列最坏 O(M×N) 空间。最大的坑：新颜色等于原始颜色时必须直接返回原图，否则「改完仍符合条件」会无限循环。这题是 200、994 等一大批网格题的地基。' },
        { id: 994, title: '腐烂的橘子', slug: 'rotting-oranges', level: '中等',
          why: '多源 BFS 模板：所有腐烂橘子同时入队，「一层」就是「一分钟」。',
          hint: '把所有初始腐烂的橘子在第 0 分钟全部入队（本节「多源 BFS」段落的原型），逐层扩散；最后检查有没有新鲜橘子幸存。',
          insight: ['多源 BFS：全部腐烂源初始入队，答案是最后一个新鲜橘子被感染时的层数，O(M×N) 时间空间。', '两个坑：一开始就没有新鲜橘子时应返回 0 而不是 -1；结束后必须确认没有幸存的新鲜橘子，有则返回 -1。预先统计新鲜橘子总数、感染一个减一个，能省掉最后的全图扫描。'] },
        { id: 127, title: '单词接龙', slug: 'word-ladder', level: '困难',
          why: '练「把隐式图抽象出来」：单词是点、改一个字母可达是边，最短转换序列就是无权图最短路。',
          hint: '不要预先两两比较建图（O(N² × L) 就没了）。对当前单词枚举「改哪一位、改成哪个字母」，去 wordList 的哈希集合里查存在性，这就是现成的隐式邻接表。',
          insight: ['BFS 逐层扩展，第一次到达 endWord 时的层数即答案，O(N × L × 26) 时间 O(N × L) 空间。', '坑一：endWord 不在字典里直接返回 0；坑二：题目要的是序列的**点数**而不是变换次数，起点也算一个；坑三：访问过的单词要立刻从集合中删除，否则反复入队直接超时。进阶：双向 BFS 每次扩展较小的一侧，实战快一个量级。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     3. DFS：一条路走到黑
     ------------------------------------------------------------ */
  'graph-dfs': {
    lede: '深度优先搜索沿一条路走到底，走不动了才回头。它不擅长最短路，但在连通性、找环、拓扑序这些「结构性」问题上是绝对主力。',
    blocks: [
      { t: 'h2', x: '走迷宫的另一种人格' },
      { t: 'p', x: 'BFS 是稳健派，一圈圈稳步推进；DFS 是莽夫，见路就钻，撞了南墙才**回溯**。它的实现几乎不用写：递归函数天生就是 DFS，因为**调用栈**替你记住了「回头路」——每层递归压一帧，回溯就是函数返回。这也直接解释了 DFS 和 BFS 的本质区别：一个用栈（后发现的先处理），一个用队列（先发现的先处理）。动画里注意看灰色回溯的时机：只有当前点的所有邻居都访问过了，才会退回上一层。' },
      { t: 'viz', kind: 'graph-dfs' },
      { t: 'h2', x: '第一个应用：数连通分量' },
      { t: 'p', x: '从某点跑一遍 DFS，能到的点全会被染色——这恰好是一个连通分量。于是数连通分量的办法水到渠成：遍历所有点，遇到没染色的就从它起跑一次 DFS，起跑次数就是答案。网格版就是经典的「岛屿数量」：' },
      { t: 'code', lang: 'cpp', name: 'islands.cpp（岛屿数量，可直接编译）', x: `#include <cstdio>
using namespace std;

int n = 4, m = 5;
char grid[4][6] = {        // '1' 陆地，'0' 海水
    "11000",
    "11000",
    "00100",
    "00011"
};

void dfs(int x, int y) {
    if (x < 0 || x >= n || y < 0 || y >= m) return;  // 越界
    if (grid[x][y] != '1') return;                    // 海水或已访问
    grid[x][y] = '2';               // 直接在网格上标记，省一个 visited 数组
    dfs(x - 1, y); dfs(x + 1, y);
    dfs(x, y - 1); dfs(x, y + 1);
}

int main() {
    int cnt = 0;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            if (grid[i][j] == '1') {
                cnt++;              // 每次「起跑」= 发现一座新岛
                dfs(i, j);          // 把整座岛淹掉
            }
    printf("岛屿数量: %d\\n", cnt);   // 输出 3
    return 0;
}` },
      { t: 'h2', x: 'DFS 树与返祖边' },
      { t: 'p', x: '把 DFS 实际走过的边画出来，会得到一棵**DFS 树**；剩下那些没走的边（无向图中）都连向自己在树上的祖先，叫**返祖边**（back edge）。这个视角值得刻在脑子里：**发现一条返祖边，等价于图里有环**——从祖先沿树边下来、再沿返祖边跳回去，就是一个环。后面 Tarjan 求强连通分量、找割点桥，全部建立在「树边 + 返祖边」这套语言上，现在混熟，后面就不慌。' },
      { t: 'callout', kind: 'note', title: '有向图的边分类更细', x: '有向图的 DFS 会把非树边分成返祖边、前向边、横叉边三类，其中**只有返祖边意味着有环**。判有向图是否有环的经典写法是「三色标记」：白=未访问，灰=在当前递归栈上，黑=已完成；DFS 时撞到**灰**点即有环。下一节拓扑排序会用到这个结论。' },
      { t: 'h2', x: '递归改栈：防爆栈的保命写法' },
      { t: 'p', x: '递归 DFS 每层要消耗调用栈，深度 10^5 以上（长链图、大网格）就可能栈溢出。把递归换成显式栈，逻辑不变，深度只受堆内存限制：' },
      { t: 'code', lang: 'cpp', name: '显式栈 DFS 片段', x: `#include <stack>
// 与递归版访问顺序略有出入（邻居出栈顺序相反），但遍历与计数结果等价
void dfsIter(int s, vector<vector<int>>& g, vector<int>& vis) {
    stack<int> st;
    st.push(s);
    while (!st.empty()) {
        int u = st.top(); st.pop();
        if (vis[u]) continue;      // 同一点可能被压多次，出栈时再过滤
        vis[u] = 1;
        for (int v : g[u])
            if (!vis[v]) st.push(v);
    }
}` },
      { t: 'callout', kind: 'warn', title: '两个高频翻车点', x: [
        '**忘记标记已访问**：图里有环时 DFS 会在环上永远打转直到爆栈。递归版进函数先标记，显式栈版出栈时判断，两种姿势选一种贯彻到底。',
        '**大网格用递归**：500×500 的全陆地网格，递归深度可达 2.5×10^5，默认栈说崩就崩。比赛遇到大网格，要么改显式栈，要么心里清楚自己在赌栈够深。',
      ] },
      { t: 'quiz',
        q: '无向连通图有 V 个点、E 条边，对它做一次完整 DFS，树边和返祖边各有多少条？',
        opts: [
          '树边 E 条，返祖边 0 条',
          '树边 V 条，返祖边 E-V 条',
          '树边 V-1 条，返祖边 E-V+1 条',
          '无法确定',
        ],
        answer: 2,
        explain: 'DFS 树覆盖全部 V 个点，任何树都恰有 V-1 条边；剩下 E-(V-1) 条全是返祖边。顺带得到一个漂亮推论：E > V-1 的连通无向图必有环——多出来的每条边都制造一个环。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: 'DFS = 栈（递归即隐式栈）+ 访问标记；数连通分量 = 数「起跑」次数；返祖边 = 有环；深图防爆栈用显式栈。BFS 管「多远」，DFS 管「通不通、有没有环」——分工记住，选型不纠结。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '从数周长到数岛屿再到逆向搜索，把「网格 DFS + 起跑计数」练透。', x: [
        { id: 463, title: '岛屿的周长', slug: 'island-perimeter', level: '简单',
          why: '网格遍历热身：练边界判断——周长就是陆地格朝海水或边界暴露的面数。',
          hint: '逐格算贡献：每个陆地格自带 4 条边，每有一个陆地邻居就少暴露 1 条。也可以 DFS 整座岛，每次走到越界或海水就给周长 +1。',
          insight: '直接遍历每个陆地格、数四个方向里是边界或海水的面数，O(M×N) 时间 O(1) 空间，不必真的递归。公式解法：周长 = 陆地数×4 - 相邻陆地对数×2。坑：数相邻对只朝右、下两个方向数，四个方向都数会翻倍。' },
        { id: 200, title: '岛屿数量', slug: 'number-of-islands', level: '中等',
          why: '「数连通分量 = 数 DFS 起跑次数」的经典原题，本节 islands.cpp 的正式版。',
          hint: '扫描全图，遇到未访问的陆地就答案 +1，并从它 DFS 把整座岛标记掉。本节示例代码就是完整思路。',
          insight: ['每个格子至多访问一次，O(M×N) 时间，递归栈最坏 O(M×N)。把访问过的 `1` 就地改成别的字符，可以省掉 visited 数组。', '坑：全陆地的大网格递归深度可达 M×N，会爆栈——正是本节「显式栈」段落的实战场景，写递归版之前先看一眼数据范围。'] },
        { id: 417, title: '太平洋大西洋水流问题', slug: 'pacific-atlantic-water-flow', level: '中等',
          why: '练逆向搜索：对每个格子正向模拟水流会重复计算到超时，从海洋边界反着爬山才是正解。',
          hint: '反过来想：从太平洋沿岸的所有格子出发，只往**不低于**当前高度的邻格走，标记「太平洋能倒灌到」的格子；大西洋同理；两个标记都有的格子就是答案。',
          insight: ['两遍多源 DFS/BFS 分别从两条海岸线出发，条件是邻格高度 ≥ 当前格，最后取交集，O(M×N) 时间空间。', '坑一：逆向之后比较方向容易写反——是往高处或等高处爬；坑二：边界格初始就属于对应海洋，要全部作为起点。这种「从目标反着搜」的手法在图题里反复出现，值得单独记一笔。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     4. 拓扑排序：把依赖排成一条线
     ------------------------------------------------------------ */
  'graph-topo': {
    lede: '先修课、编译依赖、工程排期——凡是「做 A 之前必须先做 B」的问题，都是在有向无环图上求一个合法执行顺序：拓扑排序。',
    blocks: [
      { t: 'h2', x: '依赖关系与 DAG' },
      { t: 'p', x: '把「B 依赖 A」画成有向边 A→B，整个任务网就成了有向图。若这张图存在环——A 等 B、B 等 C、C 又等 A——那谁也别想开工，问题无解。所以合法的前提是图**无环**，即 DAG（有向无环图）。**拓扑排序**就是给 DAG 的点排一个序，使每条边都从前面的点指向后面的点。它一般不唯一：互不依赖的任务谁先谁后都行。' },
      { t: 'h2', x: 'Kahn 算法：反复摘「无依赖」的点' },
      { t: 'p', x: '思路直白得像日常做事：**入度为 0** 的点没有任何前置，直接开工。做完它，它指向的点少了一个依赖（入度减一）；谁的入度降到 0，谁就获得开工资格进队列。重复到队列为空。动画里盯住两个东西：每个点头顶的入度数字，以及队列的进出节奏。' },
      { t: 'viz', kind: 'graph-topo' },
      { t: 'p', x: '这个流程还白送一个**判环器**：环上的点互相供养入度，谁都降不到 0，永远进不了队列。所以结束后数一数出队点数——**等于 V 则无环，小于 V 则必有环**。经典的「课程表」问题就是这么判的：' },
      { t: 'code', lang: 'cpp', name: 'course_schedule.cpp（课程表：拓扑序 + 判环，可直接编译）', x: `#include <cstdio>
#include <vector>
#include <queue>
using namespace std;

int main() {
    int n = 6;                                   // 课程 0~5
    // {a,b} 表示先修 a 才能修 b，即边 a->b
    vector<pair<int,int>> pre = {{0,1},{0,2},{1,3},{2,3},{3,4},{4,5}};

    vector<vector<int>> g(n);
    vector<int> indeg(n, 0);
    for (auto [a, b] : pre) {
        g[a].push_back(b);
        indeg[b]++;                              // 建图时顺手统计入度
    }

    queue<int> q;
    for (int i = 0; i < n; i++)
        if (indeg[i] == 0) q.push(i);            // 所有零依赖课程先入队

    vector<int> order;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        order.push_back(u);                      // u 正式“开工”
        for (int v : g[u])
            if (--indeg[v] == 0) q.push(v);      // 依赖清零，获得资格
    }

    if ((int)order.size() < n) {
        printf("存在循环依赖，无法排课\\n");
    } else {
        printf("一个可行的修课顺序: ");
        for (int u : order) printf("%d ", u);    // 如 0 1 2 3 4 5
        printf("\\n");
    }
    return 0;
}` },
      { t: 'h2', x: '另一条路：DFS 后序逆序' },
      { t: 'p', x: 'DFS 也能得到拓扑序：对每个点跑 DFS，在**递归返回时**（后序）把点压入结果，最后整体翻转。为什么对？一条边 u→v 中，v 要么被 u 的递归带着先访问完、先入结果，要么早就访问完了——总之 v 的后序位置一定在 u 之前，翻转后 u 就排在 v 前面，正是拓扑序要求的方向。用 DFS 法时需要配三色标记判环（撞到灰点说明有环）。两条路线怎么选：Kahn 直观、迭代实现、还能顺便数出队点数判环，工程与做题都推荐；DFS 法代码更短，且与下一大节 Tarjan 的「时间戳」思想同源，值得都认识。' },
      { t: 'callout', kind: 'warn', title: '三个易错点', x: [
        '**边的方向建反**：「先修 a 才能修 b」是 a→b。方向一反，输出恰好是倒序，样例弱时甚至能蒙对，坑就坑在这。',
        '**判环只看队列空不空**：队列空只说明流程结束，必须比较 `order.size()` 与 V 才能下无环的结论。',
        '**重边把入度加了两次**：题目可能给重复的依赖对，严谨做法是建图前去重，否则入度虚高，对应点永远降不到 0，被误判成环。',
      ] },
      { t: 'quiz',
        q: '一个 DAG 恰好有唯一的拓扑序，等价于下面哪个条件？',
        opts: [
          '图是一棵树',
          '任意时刻队列里最多只有一个入度为 0 的点',
          '所有点的入度都不超过 1',
          '图是连通的',
        ],
        answer: 1,
        explain: '唯一拓扑序意味着每一步都没有选择余地——Kahn 过程中队列任何时刻至多一个候选。等价说法：拓扑序里相邻两点之间都有直接边相连（存在一条哈密顿路径）。树和「入度 ≤ 1」都不能保证这一点：两个叶子谁先输出都行，拓扑序就不唯一了。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: '拓扑排序只对 DAG 有意义。Kahn = 入度表 + 队列，反复摘入度 0 的点；出队数 < V 即有环。DFS 后序逆序是等价路线。看到「先后依赖」四个字，第一反应就该是它。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '课程表两连是拓扑排序的户口本，第三题体验「按拓扑序递推」这一 DAG 上的通用套路。', x: [
        { id: 207, title: '课程表', slug: 'course-schedule', level: '中等',
          why: '拓扑排序判环的原题：能修完所有课 ⟺ 依赖图无环，本节 course_schedule.cpp 讲的就是它。',
          hint: '照 Kahn 模板写：建图时注意「先修 a 才能修 b」是边 a→b；跑完比较出队点数和课程总数。',
          insight: 'Kahn 算法 O(V+E) 时间空间：出队数等于 n 即无环。DFS 三色标记（撞灰点 = 有环）是等价解法。坑：题目给的 prerequisites 是 `[b, a]` 表示先 a 后 b，边的方向最容易建反——本节「易错点」第一条的原型。' },
        { id: 210, title: '课程表 II', slug: 'course-schedule-ii', level: '中等',
          why: '在判环基础上要求输出一个合法拓扑序，验证你真的会收集 order 数组。',
          hint: '和 207 同一套代码，出队时把点追加进结果数组；发现环时按题意返回空数组。',
          insight: ['Kahn O(V+E)：出队顺序就是答案，`order.size() < n` 时返回空。用 DFS 法则要后序入栈再整体翻转，别忘了翻转这步。', '坑：判环不能只看队列空——队列空只是流程结束，必须数出队点数，这是本节反复敲打的第二个易错点。'] },
        { id: 1857, title: '有向图中最大颜色值', slug: 'largest-color-value-in-a-directed-graph', level: '困难',
          why: '练「先拓扑、后 DP」：在拓扑序上递推每条路径的颜色计数，环检测与 DP 一次完成。',
          hint: '设 `f[v][c]` = 以 v 结尾的路径中颜色 c 的最多出现次数。按 Kahn 出队顺序转移：u 出队时把 `f[u]` 推给每个后继 v（v 自己的颜色到 v 时再 +1）。有环直接返回 -1。',
          insight: ['拓扑序保证转移无后效性，O((V+E)×26) 时间 O(V×26) 空间。出队数 < V 即有环返回 -1——判环和 DP 共用同一次遍历。', '坑：`f[v][color(v)]` 的 +1 要在 v 入队（所有前驱转移完）时计，转移途中就加会重复；答案取所有点所有颜色的最大值，别只看出度为 0 的点。这题就是下一大节 SCC 缩点后「DAG 上 DP」流水线的预演。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     5. Dijkstra：贪心式最短路
     ------------------------------------------------------------ */
  'graph-dijkstra': {
    lede: '边有了长短，BFS 的「层数」不再等于「距离」。Dijkstra 用一个优先队列接管出场顺序，把 BFS 平滑升级成带权图最短路。',
    blocks: [
      { t: 'h2', x: 'BFS 在带权图上哪里失灵' },
      { t: 'p', x: '设想 s 到 a 有条长 10 的直达边，s 到 b 长 1、b 到 a 也长 1。BFS 按「边数」办事，会先扩到 a 并把距离锁定成 10——而真正的最短路是绕 b 走的 2。病根在于：普通队列按**入队先后**出队，可带权图里先被发现的点不一定离得近。药方也就一句话：把队列换成**优先队列**，每次弹出「当前累计距离最小」的点。这就是 Dijkstra——它和 BFS 的代码结构几乎逐行对应，只是换了容器、比较的东西从层数变成了距离。' },
      { t: 'h2', x: '贪心为什么是对的' },
      { t: 'p', x: '核心断言：**堆顶弹出的点，它的距离已是最终答案，之后不可能再被更新**。反证一下：设堆顶弹出 u，距离 d[u]，假如还存在一条更短的路能到 u，这条路必然从已确定的区域跨出一条边、先经过某个未确定的点 y。但 y 还躺在堆里，它的距离 ≥ d[u]（否则堆顶就是 y 了），而**边权非负**，路径只会越走越长，所以那条「更短的路」≥ d[y] ≥ d[u]，矛盾。注意论证里「边权非负」用在哪一步——这正是 Dijkstra 的命门，等会儿回来收拾它。先看动画：绿色是已敲定的点，堆里是候选者，注意每次弹的都是全场最小。' },
      { t: 'viz', kind: 'graph-dijkstra' },
      { t: 'h2', x: '堆优化实现：惰性删除' },
      { t: 'p', x: '`priority_queue` 不支持修改堆里已有的元素，怎么办？不改了——发现更短路径就**再塞一份新的**，旧记录留在堆里变成垃圾；出堆时用一句 `d > dist[u]` 识别并跳过过期记录。这个技巧叫**惰性删除**，是 C++ 手写 Dijkstra 的标准姿势：' },
      { t: 'code', lang: 'cpp', name: 'dijkstra.cpp（堆优化 + 惰性删除，可直接编译）', x: `#include <cstdio>
#include <vector>
#include <queue>
using namespace std;
const long long INF = 1e18;

int main() {
    int n = 5;
    vector<vector<pair<int,int>>> g(n);          // {邻居, 边权}
    auto add = [&](int u, int v, int w) {
        g[u].push_back({v, w});
        g[v].push_back({u, w});                  // 无向图；有向图删掉这行
    };
    add(0,1,4); add(0,2,1); add(2,1,2);          // 0->1 直走 4，绕 2 只要 3
    add(1,3,1); add(2,3,5); add(3,4,3);

    vector<long long> dist(n, INF);
    // 小根堆：pair<累计距离, 点>，distance 在前才能按距离排序
    priority_queue<pair<long long,int>,
                   vector<pair<long long,int>>,
                   greater<>> pq;
    dist[0] = 0;
    pq.push({0, 0});

    while (!pq.empty()) {
        auto [d, u] = pq.top(); pq.pop();
        if (d > dist[u]) continue;               // 惰性删除：过期记录直接扔
        for (auto [v, w] : g[u])
            if (dist[u] + w < dist[v]) {         // 松弛
                dist[v] = dist[u] + w;
                pq.push({dist[v], v});           // 塞新副本，不管旧的
            }
    }

    for (int i = 0; i < n; i++)
        printf("0 -> %d 最短距离 = %lld\\n", i, dist[i]);
    // 结果: 0, 3, 1, 4, 7
    return 0;
}` },
      { t: 'p', x: '复杂度 `O(E log E)`：每条边最多触发一次入堆，每次堆操作 log。垃圾副本让堆的规模从 V 涨到 E，log 差个常数，换来实现上的大幅简化，这笔交易稳赚。' },
      { t: 'h2', x: '负权边如何破坏它' },
      { t: 'p', x: '回看正确性论证——「路径只会越走越长」依赖每条边权 ≥ 0。放进一条负权边，「先绕远、再用负边扣回来」就可能反超已敲定的答案，而 Dijkstra 弹出即定案、绝不回头。例：s→a 权 2，s→b 权 3，b→a 权 -2。带 visited 数组的教科书版会先把 a 以距离 2 定案，之后拒绝一切更新，永远错过真实最短的 1。细心的你可能会说：上面的惰性删除版没有 visited，a 会被重新入堆「救回来」——确实，但那一刻它已不是 Dijkstra：定案承诺失效，同一个点可能被反复重算，最坏退化到指数级，遇上负环更是永不停机。所以别赌：' },
      { t: 'callout', kind: 'danger', title: '负权边 = 换算法，没有商量', x: '图里只要**可能**出现负权边，就放弃 Dijkstra，改用下一节的 Bellman-Ford / SPFA。负权下 Dijkstra 的输出没有任何保证，最阴险的是它多数时候「碰巧对」，只在特定数据上错——这种 bug 靠肉眼和弱样例基本查不出来。' },
      { t: 'callout', kind: 'warn', title: '实现层面三连坑', x: [
        '**堆里 pair 的顺序**：必须距离在前、点在后，`greater<>` 才按距离弹小的。写反了退化成按编号排序，结果直接错。',
        '**INF 溢出**：`dist[u] + w` 可能超 int。距离数组用 `long long`、INF 取 1e18 一劳永逸。',
        '**忘写惰性删除那行**：不判 `d > dist[u]` 结果仍正确，但同一个点会被反复松弛邻居，稠密图上性能可能崩到超时。',
      ] },
      { t: 'quiz',
        q: 'Dijkstra 运行中，某点 v 第二次从堆里弹出（携带的 d 大于 dist[v]），正确处理是？',
        opts: [
          '用它再松弛一遍邻居，反正不亏',
          '直接 continue 跳过，这是过期副本',
          '说明算法写错了，不该有第二次',
          '更新 dist[v] 为这个较大的 d',
        ],
        answer: 1,
        explain: '惰性删除的设计就是允许同一点多份记录并存，真身只有最小那份；后弹出的都是过期垃圾，一句 `if (d > dist[u]) continue;` 送走。选 A 不影响正确性但白费时间，选 D 是把答案改坏。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: 'Dijkstra = BFS 把队列换成小根堆；弹出即定案，靠「边权非负」撑腰；实现记三件套——pair 距离在前、long long 防溢出、`d > dist[u]` 惰性删除。见到负权边，立刻换 Bellman-Ford。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '第一题默写模板，后两题把「累计距离」换成别的度量——检验你是否真懂堆里比较的是什么。', x: [
        { id: 743, title: '网络延迟时间', slug: 'network-delay-time', level: '中等',
          why: '堆优化 Dijkstra 的模板题：单源最短路跑完取最大值，检验建图 + 惰性删除全流程。',
          hint: '从 k 跑单源最短路，答案是所有点距离的最大值（信号传遍全网的时刻）；有点不可达则返回 -1。照本节 dijkstra.cpp 默写。',
          insight: '堆优化 Dijkstra，O(E log E) 时间 O(V+E) 空间。坑集中在三件套：堆里 pair 必须距离在前；`d > dist[u]` 惰性删除别漏；节点编号 1~n，数组开 n+1。答案取 max 之前先确认没有 INF。' },
        { id: 1631, title: '最小体力消耗路径', slug: 'path-with-minimum-effort', level: '中等',
          why: '练 Dijkstra 的变形：路径代价从「边权之和」换成「边权最大值」，松弛公式跟着变。',
          hint: '把相邻格子高度差的绝对值当边权，路径代价定义为沿途边权的**最大值**。Dijkstra 骨架不动，松弛从 `d[u]+w` 改成 `max(d[u], w)`。',
          insight: ['变形 Dijkstra：`max(d[u], w) < d[v]` 时松弛，正确性依旧靠「弹出即定案」，O(M×N log(M×N)) 时间。', '坑：网格题别忘了它本质是 4 邻接的隐式图，不用真建邻接表。等价解法有两个——二分答案 + BFS 判可达、按边权排序 + 并查集（第一次连通起终点时的边权即答案），三种都值得写一遍。'] },
        { id: 778, title: '水位上升的泳池中游泳', slug: 'swim-in-rising-water', level: '困难',
          why: '同族进阶：代价是「路径上格子高度的最大值」，练把问题翻译成瓶颈最短路的能力。',
          hint: '到达某格的代价 = 途经格子高度的最大值（水位涨到多高才能游过去）。还是瓶颈型 Dijkstra：松弛用 `max(d[u], height[v])`。',
          insight: ['瓶颈 Dijkstra O(N² log N)；二分水位 + BFS/DFS 判连通是 O(N² log N) 的等价路线；按高度从小到大灌水 + 并查集（起终点连通时的高度即答案）同样漂亮。', '坑：起点 (0,0) 的初始代价是 `grid[0][0]` 而不是 0——水位至少要漫过起点你才下得了水。这题和 1631 放在一起对比，能看清「求和型」与「瓶颈型」最短路的唯一差别就在松弛公式。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     6. Bellman-Ford / SPFA / Floyd
     ------------------------------------------------------------ */
  'graph-bellman': {
    lede: '负权边把 Dijkstra 拉下马之后，接棒的是三位选手：逐轮松弛的 Bellman-Ford、它的队列优化 SPFA、以及全源最短路的 Floyd。这一节顺便把「选哪个」一次说清。',
    blocks: [
      { t: 'h2', x: 'Bellman-Ford：不贪心，用轮次换正确性' },
      { t: 'p', x: 'Dijkstra 靠贪心抢时间，负权下翻车；Bellman-Ford 反其道而行——不挑不拣，**把所有边整体松弛一轮，重复 V-1 轮**。为什么 V-1 轮够？最短路不含环时至多 V-1 条边，而**第 k 轮结束后，所有「最短路用 ≤ k 条边」的点都已到位**（归纳：第 k 轮会把第 k-1 轮的成果沿每条边再推一步）。松弛顺序、贪不贪心都无所谓，轮数管够就对。代价是 `O(VE)`，比 Dijkstra 慢一个量级——这是为负权付的保险费。动画里按轮观察：每一轮之后，又一批点的距离尘埃落定。' },
      { t: 'viz', kind: 'graph-bellman' },
      { t: 'h2', x: '负环检测：第 V 轮的意义' },
      { t: 'p', x: 'V-1 轮理应收工。若**第 V 轮仍有边能松弛成功**，说明有条「最短路」用到了 ≥ V 条边——路径上必有重复点，即有环，且绕这一圈距离还变小了：**负环**。沿负环兜圈子距离无限下降，最短路根本不存在。Bellman-Ford 是负环的标准检测器：' },
      { t: 'code', lang: 'cpp', name: 'bellman_ford.cpp（最短路 + 负环检测，可直接编译）', x: `#include <cstdio>
#include <vector>
using namespace std;
const long long INF = 1e18;

struct Edge { int u, v, w; };

int main() {
    int n = 5;
    vector<Edge> es = {                     // 有向图，含负权边（但无负环）
        {0,1,4}, {0,2,5}, {1,3,3},
        {2,1,-4},                            // 负权边：绕道 2 再回 1 更便宜
        {3,4,2}, {2,4,9},
    };

    vector<long long> dist(n, INF);
    dist[0] = 0;

    for (int round = 1; round <= n - 1; round++)   // V-1 轮
        for (auto& e : es)
            if (dist[e.u] != INF && dist[e.u] + e.w < dist[e.v])
                dist[e.v] = dist[e.u] + e.w;       // 松弛

    bool negCycle = false;                   // 第 V 轮：还能松弛 => 负环
    for (auto& e : es)
        if (dist[e.u] != INF && dist[e.u] + e.w < dist[e.v])
            negCycle = true;

    if (negCycle) printf("图中存在负环，最短路无定义\\n");
    else
        for (int i = 0; i < n; i++)
            printf("0 -> %d = %lld\\n", i, dist[i]);
    // 结果: 0, 1, 5, 4, 6（去 1 的最短路是 0->2->1 = 5-4 = 1）
    return 0;
}` },
      { t: 'callout', kind: 'warn', title: '必须先判 dist[u] != INF', x: '松弛前不判 `dist[e.u] != INF`，「不可达点」会拿 INF 加边权去更新别人——INF + 负数小于 INF，凭空造出一串假距离，还可能连锁污染、误报负环。这是 Bellman-Ford 第一大 bug 源。' },
      { t: 'h2', x: 'SPFA：只叫醒该干活的' },
      { t: 'p', x: '朴素版每轮盲扫全部边，但其实**只有上一轮距离变过的点，才可能松弛出新东西**。SPFA 的全部改动：拿个队列装「刚被更新的点」，出队松弛邻居，谁被更新谁入队（在队的不重复入）。稀疏图随机数据上快得像 O(E)，于是江湖人送外号「关于 SPFA——它死了」：**最坏仍是 O(VE)**，特定构造（网格图、链上挂菊花）能把它卡回原形。出题人深谙此道，正权图请老实用 Dijkstra，SPFA 只在「有负权、图别太刁」时上场。判负环的姿势也变了：某个点**入队超过 V 次**（或松弛出的路径长度 ≥ V 条边）即有负环。' },
      { t: 'h2', x: 'Floyd：全源最短路的三重循环' },
      { t: 'p', x: '要**任意两点**之间的最短路时，换 DP 思路。`f[i][j]` 初始为邻接矩阵，按顺序放开中转点：外层枚举 k，表示「现在允许经过前 k 个点中转」，转移 `f[i][j] = min(f[i][j], f[i][k] + f[k][j])`。三重循环五行代码，`O(V³)`，V ≤ 400 左右可用，负权边照吃（负环仍无解，特征是跑完后某个 `f[i][i] < 0`）：' },
      { t: 'code', lang: 'cpp', name: 'Floyd 核心（k 必须在最外层）', x: `for (int k = 0; k < n; k++)          // 中转点在最外层，动过就错
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            if (f[i][k] + f[k][j] < f[i][j])
                f[i][j] = f[i][k] + f[k][j];

// 传递闭包：把「距离」换成「可达」，min/+ 换成 |/&&
// reach[i][j] |= reach[i][k] && reach[k][j];  可用 bitset 再提速 64 倍` },
      { t: 'p', x: '把矩阵里的数值换成布尔「能不能到」，同一套循环就变成**传递闭包**——回答「i 是否可达 j」这类连通性问题，这也是邻接矩阵在本模块里最风光的时刻。' },
      { t: 'h2', x: '三种算法怎么选' },
      { t: 'table',
        head: ['场景', '选择', '复杂度', '理由'],
        rows: [
          ['单源、边权全非负', 'Dijkstra（堆）', '`O(E log E)`', '最快且稳，默认首选'],
          ['单源、有负权边', 'Bellman-Ford / SPFA', '`O(VE)`', '唯一能扛负权的单源选择'],
          ['需要检测负环', 'Bellman-Ford', '`O(VE)`', '第 V 轮仍可松弛即负环'],
          ['任意两点、V ≤ 400', 'Floyd', '`O(V³)`', '五行写完，全源一把出'],
          ['边权只有 0 和 1', '0-1 BFS', '`O(V+E)`', '双端队列白捡线性'],
        ] },
      { t: 'quiz',
        q: '图里存在负权边但确定没有负环，求单源最短路，哪个方案正确又不浪费？',
        opts: [
          'Dijkstra——没有负环它就是对的',
          '给所有边加同一个常数变成非负，再跑 Dijkstra',
          'Bellman-Ford（或 SPFA）',
          '只能用 Floyd',
        ],
        answer: 2,
        explain: 'Dijkstra 怕的是负权**边**本身，跟有没有负环是两回事（上一节的三点反例就没有环）。加常数更是经典陷阱：边数多的路径被多罚好几份常数，最短路径可能因此易主。全源的 Floyd 能做但杀鸡用牛刀。正解就是 Bellman-Ford 系。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: 'Bellman-Ford：V-1 轮全边松弛，第 V 轮还动 = 负环；SPFA 是它的队列优化，最坏 O(VE) 会被卡；Floyd 三重循环 k 在最外层，全源 + 传递闭包。选型表建议直接背下。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '三道题各占一个角度：限制轮数的 Bellman-Ford、全源 Floyd、带权并查集式的关系传递。', x: [
        { id: 787, title: 'K 站中转内最便宜的航班', slug: 'cheapest-flights-within-k-stops', level: '中等',
          why: '练 Bellman-Ford 「按轮数递推」的本质：最多中转 k 站 = 最多松弛 k+1 轮。',
          hint: '回想「第 k 轮结束后，所有最短路用 ≤ k 条边的点都已到位」——把轮数上限从 V-1 改成 k+1 就是答案。Dijkstra 在这里反而不好使：边数限制破坏了「弹出即定案」。',
          insight: ['跑 k+1 轮全边松弛，O(K×E) 时间。核心坑：每轮必须基于**上一轮的快照**转移（松弛前复制一份 dist），否则一轮内会连锁松弛、变相多用中转次数，答案偏小。', '这题是「轮数 = 边数上限」这一 Bellman-Ford 语义最直接的应用，比背模板更重要。'] },
        { id: 1334, title: '阈值距离内邻居最少的城市', slug: 'find-the-city-with-the-smallest-number-of-neighbors-at-a-threshold-distance', level: '中等',
          why: 'Floyd 模板题：n ≤ 100 的全源最短路，五行三重循环直接出结果。',
          hint: '要每对城市间的最短路，点数只有 100——正是选型表里「任意两点、V ≤ 400 用 Floyd」那一格。跑完逐城市数阈值内的邻居。',
          insight: 'Floyd O(V³) 时间 O(V²) 空间。坑一：k 必须在最外层，动过就错——这是本节反复强调的那一行；坑二：无向图初始化矩阵时两个方向都要赋值，对角线为 0，其余为 INF；坑三：并列时要返回编号**最大**的城市，从小到大扫时用 ≤ 更新即可。' },
        { id: 399, title: '除法求值', slug: 'evaluate-division', level: '中等',
          why: '练「关系传递」的图建模：a/b=2、b/c=3 推出 a/c=6，本质是带权图上的可达性 + 路径权乘积。',
          hint: '把 a/b = v 建成双向边：a→b 权 v、b→a 权 1/v。每个查询从起点 DFS/BFS 到终点，沿途把边权连乘。变量不存在或不连通就是 -1。',
          insight: ['DFS/BFS 每次查询 O(V+E)；查询多时可用 Floyd 思路预处理所有点对的比值（把 min/+ 换成乘法），或上带权并查集做到近 O(1) 查询。', '坑：自查询 a/a 只要 a 出现过就是 1.0；DFS 要带 visited 防止环上打转。这题的价值在「把 min/+ 换成任意可结合运算，Floyd 骨架照用」——传递闭包那段讲的正是这件事。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     7. 最小生成树：Kruskal 与 Prim
     ------------------------------------------------------------ */
  'graph-mst': {
    lede: '用最少的电缆把所有村庄连上电——这就是最小生成树。Kruskal 从「最便宜的边」下手，Prim 从「一个点向外长」，背后是同一条切割性质在撑腰。',
    blocks: [
      { t: 'h2', x: '问题与直觉' },
      { t: 'p', x: '连通无向带权图里，选出 V-1 条边把所有点连通、且总权最小，这棵树叫**最小生成树**（MST）。为什么一定是树？连通至少要 V-1 条边，多一条就成环——环上随便拆掉一条最贵的边，仍然连通还更省，所以最优解必然不含环。' },
      { t: 'h2', x: '切割性质：两种算法共同的靠山' },
      { t: 'p', x: '把点集任意划成两半（一个**切割**），横跨两半的边里**权最小的那条 e，必在某棵 MST 中**。反证：若最优树 T 不含 e，把 e 加进 T 会形成环，环必然还有另一条横跨两半的边 f（不然两半在 T 里就不连通了），且 w(f) ≥ w(e)。用 e 换掉 f，总权不升、依旧是生成树——换完的树同样最优。这条**cut property** 是接下来两个贪心的通用许可证：只要能把当前局面描述成一个切割，放心拿最小跨边，永远不会后悔。' },
      { t: 'h2', x: 'Kruskal：全局最便宜的边优先' },
      { t: 'p', x: '把边按权从小到大排序，逐条尝试：两端已在同一棵树里（会成环）就扔，否则收下。「在不在同一棵树」正是**并查集**的看家本领。它对应的切割是「这条边两端各自的连通块 vs 其余世界」——收下的每条边都是某个切割的最小跨边。看动画：绿色收下，红色因成环丢弃，攒够 V-1 条收工。' },
      { t: 'viz', kind: 'graph-mst-kruskal' },
      { t: 'code', lang: 'cpp', name: 'kruskal.cpp（排序 + 并查集，可直接编译）', x: `#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;

struct DSU {
    vector<int> fa;
    DSU(int n) : fa(n) { for (int i = 0; i < n; i++) fa[i] = i; }
    int find(int x) { return fa[x] == x ? x : fa[x] = find(fa[x]); }  // 路径压缩
    bool unite(int a, int b) {
        a = find(a); b = find(b);
        if (a == b) return false;            // 已同树：这条边会成环
        fa[a] = b;
        return true;
    }
};

struct Edge { int u, v, w; };

int main() {
    int n = 6;
    vector<Edge> es = {
        {0,1,4},{0,2,3},{1,2,1},{1,3,2},
        {2,3,4},{3,4,2},{4,5,6},{2,4,5},
    };
    sort(es.begin(), es.end(),
         [](const Edge& a, const Edge& b) { return a.w < b.w; });

    DSU dsu(n);
    long long total = 0;
    int used = 0;
    for (auto& e : es) {
        if (!dsu.unite(e.u, e.v)) continue;  // 成环，扔
        total += e.w;
        if (++used == n - 1) break;          // 攒够 V-1 条提前收工
    }

    if (used < n - 1) printf("图不连通，没有生成树\\n");
    else printf("MST 总权 = %lld\\n", total);   // 输出 14
    return 0;
}` },
      { t: 'h2', x: 'Prim：从一个点向外生长' },
      { t: 'p', x: 'Prim 把点分成「已并入的树」和「树外」两个阵营——天然的切割。每步取连接两个阵营的最小边，把对面那个点拉进来，重复 V-1 次。用小根堆维护候选边，出堆时发现对端已在树里就跳过——和 Dijkstra 同款的惰性删除，只是堆里放的是**边权**而非**累计距离**。' },
      { t: 'viz', kind: 'graph-mst-prim' },
      { t: 'code', lang: 'cpp', name: 'prim.cpp（堆实现，可直接编译）', x: `#include <cstdio>
#include <vector>
#include <queue>
using namespace std;

int main() {
    int n = 6;
    vector<vector<pair<int,int>>> g(n);      // {邻居, 权}
    auto add = [&](int u, int v, int w) {
        g[u].push_back({v, w});
        g[v].push_back({u, w});
    };
    add(0,1,4); add(0,2,3); add(1,2,1); add(1,3,2);
    add(2,3,4); add(3,4,2); add(4,5,6); add(2,4,5);

    vector<int> inTree(n, 0);
    priority_queue<pair<int,int>,
                   vector<pair<int,int>>, greater<>> pq;  // {边权, 点}
    long long total = 0;
    int used = 0;
    pq.push({0, 0});                          // 从 0 号点起步

    while (!pq.empty() && used < n) {
        auto [w, u] = pq.top(); pq.pop();
        if (inTree[u]) continue;              // 惰性删除：对端已并入
        inTree[u] = 1;
        total += w;                           // 起点带进来的 0 不影响总和
        used++;
        for (auto [v, wv] : g[u])
            if (!inTree[v]) pq.push({wv, v});
    }

    if (used < n) printf("图不连通\\n");
    else printf("MST 总权 = %lld\\n", total);    // 同样输出 14
    return 0;
}` },
      { t: 'h2', x: '选哪个' },
      { t: 'table',
        head: ['', 'Kruskal', 'Prim（堆）'],
        rows: [
          ['复杂度', '`O(E log E)`（瓶颈在排序）', '`O(E log V)`'],
          ['视角', '全局挑边，并查集防环', '局部生长，堆挑跨边'],
          ['擅长', '稀疏图；边已给成列表时几乎零改造', '稠密图（配矩阵可做 O(V²) 版）'],
          ['依赖的数据结构', '排序 + 并查集', '优先队列'],
        ] },
      { t: 'p', x: '做题层面：边表给得直接、要写得快，选 Kruskal；点少边爆炸的稠密图（比如完全图上建树）用 O(V²) 朴素 Prim 反而最稳。两个都会，考场看菜下饭。' },
      { t: 'callout', kind: 'warn', title: '易错清单', x: [
        '**忘判连通**：图本身不连通就没有生成树。Kruskal 数收下的边是否到 V-1，Prim 数并入的点是否到 V，缺一句判断答案就是错的。',
        '**并查集忘了路径压缩**：裸并查集最坏退化成链，`find` 变 O(V)；一行 `fa[x] = find(fa[x])` 找回近似 O(1)。',
        '**「换 min 为 max」时想当然**：求最大生成树只需把排序反过来，切割性质对称成立；但求「乘积最小」「最大边最小」等变体前，先想清楚贪心依据还在不在。',
      ] },
      { t: 'quiz',
        q: '如果图中所有边权**互不相同**，最小生成树有几棵？',
        opts: ['可能有多棵', '恰好一棵', '取决于用 Kruskal 还是 Prim', '取决于起点'],
        answer: 1,
        explain: '边权互异时 MST 唯一。反证：若有两棵不同的最优树 T1、T2，取只在 T1 的最小边 e，加入 T2 成环，环里必有一条不在 T1 的边 f；e、f 都横跨同一切割且权互异，换掉较大者得到更优或同权但矛盾的树。也因此这种图上 Kruskal 和 Prim 必然长出同一棵树——算法不同，答案殊途同归。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: '切割性质：任何切割的最小跨边必属于某棵 MST——两个算法都是它的具体化。Kruskal = 排序 + 并查集，稀疏图首选；Prim = 堆 + 惰性删除，稠密图有 O(V²) 后手。收边/收点数不够 V-1/V 记得报不连通。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '第一题在完全图上体会 Kruskal 与 Prim 的选型，第二题把「换边论证」变成代码。', x: [
        { id: 1584, title: '连接所有点的最小费用', slug: 'min-cost-to-connect-all-points', level: '中等',
          why: 'MST 模板题，且是**完全图**：正好验证选型表——稠密图上朴素 Prim 反而胜过 Kruskal。',
          hint: '任意两点都有边（曼哈顿距离为权），共 n(n-1)/2 条。Kruskal 生成全部边排序即可；也试试 O(V²) 朴素 Prim——维护每个树外点到树的最小距离数组，不用堆。',
          insight: ['Kruskal O(E log E)（E 约 n²/2，排序是大头）；朴素 Prim O(V²)，完全图上更优且省内存——正是「点少边爆炸用 Prim」的实战注脚。', '坑：Kruskal 别忘并查集的路径压缩；攒够 n-1 条边立刻 break，能省一半时间。两种都写一遍，对比才有体感。'] },
        { id: 1489, title: '找到最小生成树里的关键边和伪关键边', slug: 'find-critical-and-pseudo-critical-edges-in-minimum-spanning-tree', level: '困难',
          why: '深挖 MST 的唯一性：关键边（删了 MST 总权必变大）与伪关键边（可进可不进），逼你把切割性质和换边论证用成工具。',
          hint: '先算标准 MST 总权 W。对每条边 e 问两个问题：**强制不用** e 再跑 Kruskal，总权变大（或不连通）吗？是则 e 是关键边。**强制先用** e 再跑 Kruskal，总权仍是 W 吗？是则 e 至少是伪关键边。',
          insight: ['枚举每条边跑两次 Kruskal，O(E² × α(V)) 时间，E 只有 200 完全够。判「强制不用后不连通」等价于总权视为无穷大。', '坑：排序会打乱边的原始下标，排序前必须把编号绑在边上；关键边同时满足伪关键边的判定，先判关键、再判伪关键，别重复归类。这题做完，「边权互异时 MST 唯一」那道 quiz 就有了肌肉记忆。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     8. 连通分量与 Tarjan
     ------------------------------------------------------------ */
  'graph-scc': {
    lede: '有向图里「互相可达」的一团点叫强连通分量。Tarjan 用一次 DFS 加两个时间戳把它们全部找出来，再把每团缩成一个点，图就化简成好处理的 DAG。',
    blocks: [
      { t: 'h2', x: '为什么要找 SCC' },
      { t: 'p', x: '有向图里 u 能到 v、v 也能到 u，就说两点**强连通**；极大的强连通点集叫**强连通分量**（SCC）。它的价值在「缩点」：把每个 SCC 捏成一个超级点后，剩下的图**必然无环**——若缩点后还有环，环上那几团就能互相到达，早该并成一团了。于是任意有向图都能化归成 DAG，拓扑排序、DAG 上 DP 那套工具全部解锁。互相引用的网页集群、传递依赖成环的模块，都是 SCC 的现实原型。' },
      { t: 'h2', x: 'dfn 与 low：两个时间戳' },
      { t: 'p', x: 'Tarjan 的全部机关是给每个点记两个数。**dfn[u]**：DFS 第一次到达 u 的时间戳，出生即固定。**low[u]**：从 u 出发，走任意多条树边、再**至多一条**指回栈内的返祖/横叉边，能摸到的最小 dfn——白话就是「u 顺着子树，最早能回到哪位祖先」。递归时 low 不断向上汇报取 min。回溯到 u 时若 **low[u] == dfn[u]**，意味着 u 的整棵子树谁也翻不过 u 这道墙回到更早的祖先——u 就是一个 SCC 的「根」，此刻收网正好。' },
      { t: 'h2', x: '栈：圈住「还没定案」的点' },
      { t: 'p', x: '和普通 DFS 不同，Tarjan 额外维护一个栈：点被访问就入栈，**直到它所在的 SCC 整体定案才弹出**。栈里装的是「当前 DFS 路径上还悬而未决的候选人」。当 u 处 low[u] == dfn[u] 收网时，从栈顶一路弹到 u，弹出来的恰好是这个 SCC 的全部成员。也因此 low 只认**栈内**点的 dfn：已出栈的点属于早已定案的其他 SCC，指向它们的横叉边不构成「能回去」的证据。动画里重点核对每个点头上的 dfn/low 数字和栈的伸缩。' },
      { t: 'viz', kind: 'graph-scc' },
      { t: 'code', lang: 'cpp', name: 'tarjan_scc.cpp（可直接编译）', x: `#include <cstdio>
#include <vector>
#include <stack>
#include <algorithm>
using namespace std;

int n = 6, timer_ = 0, sccCnt = 0;
vector<vector<int>> g(n);
vector<int> dfn(n, 0), low(n, 0), inStk(n, 0), sccId(n, -1);
stack<int> stk;

void tarjan(int u) {
    dfn[u] = low[u] = ++timer_;          // 出生时间戳，low 先等于自己
    stk.push(u); inStk[u] = 1;
    for (int v : g[u]) {
        if (!dfn[v]) {                   // 树边：先递归，再吸收孩子的 low
            tarjan(v);
            low[u] = min(low[u], low[v]);
        } else if (inStk[v]) {           // 指向栈内点：能“回去”的证据
            low[u] = min(low[u], dfn[v]);
        }                                // 已出栈的点：别的 SCC，无视
    }
    if (low[u] == dfn[u]) {              // u 是本 SCC 的根，收网
        sccCnt++;
        while (true) {
            int x = stk.top(); stk.pop(); inStk[x] = 0;
            sccId[x] = sccCnt - 1;
            if (x == u) break;
        }
    }
}

int main() {
    // 两个环 0->1->2->0、3->4->3，加一条跨环边 2->3，5 孤立
    int es[6][2] = {{0,1},{1,2},{2,0},{2,3},{3,4},{4,3}};
    for (auto& e : es) g[e[0]].push_back(e[1]);

    for (int i = 0; i < n; i++)
        if (!dfn[i]) tarjan(i);          // 图可能不连通，逐点补跑

    printf("SCC 个数 = %d\\n", sccCnt);    // 3 个：{0,1,2} {3,4} {5}
    for (int i = 0; i < n; i++)
        printf("点 %d 属于 SCC %d\\n", i, sccId[i]);
    return 0;
}` },
      { t: 'p', x: '一次 DFS 搞定，复杂度 `O(V+E)`。同族的 Kosaraju 算法（正图反图各一遍 DFS）更好理解但要建反图；Tarjan 一遍过、常数小，是默认选择。' },
      { t: 'h2', x: '缩点之后的套路' },
      { t: 'p', x: '拿到 `sccId` 后，把原图每条边 u→v 映射成 `sccId[u]`→`sccId[v]`（两端同 SCC 的边丢弃，跨 SCC 的可能产生重边、按需去重），得到缩点 DAG。接下来的问题几乎都是流水线：「至少投放多少信息能传遍全图」= 数 DAG 里**入度为 0** 的超级点；「至少加几条边让全图强连通」= max(入度 0 的点数, 出度 0 的点数)（整图已强连通时答案为 0）；带点权的路径最值 = 在 DAG 上按拓扑序 DP。**先缩点、再拓扑、后 DP**，这条流水线值得当模板记住。' },
      { t: 'callout', kind: 'danger', title: '最容易写错的一行', x: '横叉边处必须是 `low[u] = min(low[u], dfn[v])`——用 v 的 **dfn**，且仅当 v **在栈内**。两个常见手滑：写成 `low[v]`（此处恰好多数数据能对，换组数据就翻车，极难排查）；漏掉 `inStk[v]` 判断（把已定案 SCC 的点当返回证据，把本该分开的分量错误合并）。默写模板后先检查这一行。' },
      { t: 'callout', kind: 'warn', title: '别忘了图可能不连通', x: '主函数必须 `for` 所有点、对没有 dfn 的补跑 tarjan。只从 0 号点跑一次，够不着的部分整个漏掉——孤立点 5 就是安排来验证这件事的。' },
      { t: 'quiz',
        q: 'Tarjan 运行中，点 u 的一条出边指向已访问但**已出栈**的点 v，应该怎么处理？',
        opts: [
          'low[u] = min(low[u], dfn[v])',
          'low[u] = min(low[u], low[v])',
          '忽略这条边',
          '把 v 重新压回栈',
        ],
        answer: 2,
        explain: '已出栈 = v 的 SCC 早已定案，u 到得了 v，但 v 那边绝无路径回到 u（有的话 v 收网时会把 u 一起收走）。这条边提供不了「回到祖先」的证据，直接无视。只有指向**栈内**点的边才参与 low 的更新。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: 'dfn = 出生时间，low = 顺子树至多接一条回边能摸到的最早祖先；low == dfn 处收网，栈里弹出的就是一个 SCC；横叉边取 dfn 且必须栈内。缩点得 DAG 后：数入度出度、拓扑序 DP，都是流水线活。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '先用连通分量计数热身，再用 Tarjan 求桥检验 dfn/low 是否真的长在了手上。', x: [
        { id: 1319, title: '连通网络的操作次数', slug: 'number-of-operations-to-make-network-connected', level: '中等',
          why: '练连通分量计数与「多余边」思维：k 个分量连成一体恰需 k-1 条线，前提是可挪的冗余线够数。',
          hint: '线不够（边数 < n-1）直接 -1。够则答案 = 连通分量数 - 1：每条「两端已连通」的多余边都能拔下来接到分量之间。数分量用 DFS 或并查集都行。',
          insight: ['并查集数分量 O(E × α(V))，或 DFS 数「起跑」次数 O(V+E)。核心结论：连通 n 个点至少 n-1 条边，多余边可任意重接。', '坑：先判 `connections.size() < n-1` 再数分量，顺序反了逻辑也能对但容易漏判。这题虽不用 Tarjan，却是「把图压缩成分量层面思考」的第一步。'] },
        { id: 1192, title: '查找集群内的关键连接', slug: 'critical-connections-in-a-network', level: '困难',
          why: 'Tarjan 求**桥**的原题：dfn/low 这套时间戳语言在无向图上的直接应用。',
          hint: '桥 = 删掉后图不连通的边。DFS 树上，若孩子 v 满足 `low[v] > dfn[u]`（注意是严格大于，没有等号），说明 v 的子树没有任何返祖边能绕回 u 或更早——边 u-v 就是桥。',
          insight: ['一次 DFS O(V+E)。与 SCC 版的两处差别：判桥用 `low[v] > dfn[u]` 而缩点判根用等号；无向图递归时要排除「直接原路返回父亲」，传父节点编号（有重边时应传入边编号）。', '坑：n 到 10^5，递归深度可能爆栈——正是 DFS 那节「显式栈保命」的实战场景。写完可顺手对比「割点」的判法（`low[v] >= dfn[u]`，根节点特判），一套时间戳三种收获。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     9. 二分图判定与匈牙利匹配
     ------------------------------------------------------------ */
  'graph-bipart': {
    lede: '点能分成两个阵营、边只跨阵营不内斗，就是二分图。判定靠两色染色，配对靠匈牙利算法——核心动作是一句「你能不能挪个位置让给我」。',
    blocks: [
      { t: 'h2', x: '定义与判定：染色法' },
      { t: 'p', x: '**二分图**：顶点可划分为左右两个集合，每条边的两端分属两边。员工-岗位、学生-课题、用户-物品，凡是「两类对象之间的关系」天生就是二分图。判定不需要真的找出划分，**染色**即可：BFS/DFS 遍历，起点染黑，邻居染白，邻居的邻居染黑……若某条边两端撞了同色，则不是二分图。每个连通分量都要染一遍。复杂度 `O(V+E)`。' },
      { t: 'h2', x: '奇环：唯一的绊脚石' },
      { t: 'p', x: '**图是二分图 ⟺ 图中没有奇数长度的环**。正向：二分图里任何环必然左右左右交替横跳，回到起点必是偶数步。反向：没有奇环时染色为什么不会失败？取任意生成树按层染色（偶数层黑、奇数层白），任何非树边若连接同色点，则它与树上路径拼成的环长为奇数——与无奇环矛盾。所以染色报冲突 = 抓到了奇环，二者是一回事。三角形是最小的奇环，所以含三角形的图永远不是二分图。' },
      { t: 'h2', x: '匹配与增广路：「腾位置」的艺术' },
      { t: 'p', x: '**匹配**：一组两两不共端点的边——每人至多牵一个岗位。**最大匹配**要让牵手对数最多。匈牙利算法逐个给左边的点找对象，遇到心仪岗位已被占，不是放弃，而是礼貌地问占用者：「**你还有别的岗位能挪过去吗？**」占用者递归地问自己的下家……只要这条「挪位链」在某处找到空位，整条链集体挪一格，成功多配一对。这条链的学名是**增广路**：从未匹配点出发，非匹配边与匹配边交替出现，终点也未匹配；把路上每条边的身份翻转（匹配变非匹配、非匹配变匹配），匹配数恰好 +1。动画里看虚线试探、整链翻转的瞬间：' },
      { t: 'viz', kind: 'graph-bipartite' },
      { t: 'code', lang: 'cpp', name: 'hungarian.cpp（染色判定 + 匈牙利匹配，可直接编译）', x: `#include <cstdio>
#include <vector>
#include <cstring>
using namespace std;

int nl = 4, nr = 4;                  // 左 4 人，右 4 岗
vector<vector<int>> g(nl);           // g[u] = 左 u 心仪的岗位列表
int matchR[4];                       // matchR[v] = 岗位 v 现在归谁，-1 空
bool vis[4];                         // 本轮增广中岗位 v 是否已被问过

bool tryMatch(int u) {               // 给左 u 找岗位，找到返回 true
    for (int v : g[u]) {
        if (vis[v]) continue;        // 本轮问过，别循环纠缠
        vis[v] = true;
        // 岗位空着，或占用者能挪去别处 —— 都成
        if (matchR[v] == -1 || tryMatch(matchR[v])) {
            matchR[v] = u;           // 链条翻转的落点：v 归 u
            return true;
        }
    }
    return false;                    // 心仪列表问遍，本轮无路
}

int main() {
    // 人 0:{0,1} 人 1:{0} 人 2:{1,2} 人 3:{2,3}
    g[0] = {0, 1}; g[1] = {0}; g[2] = {1, 2}; g[3] = {2, 3};

    memset(matchR, -1, sizeof matchR);
    int ans = 0;
    for (int u = 0; u < nl; u++) {
        memset(vis, 0, sizeof vis);  // 每人开问前清空「问过」标记
        if (tryMatch(u)) ans++;
    }
    printf("最大匹配数 = %d\\n", ans);   // 输出 4：0->1, 1->0, 2->2... 挪出全配
    return 0;
}` },
      { t: 'p', x: '注意人 1 只认岗位 0：轮到他时岗位 0 已被人 0 占下，`tryMatch(0)` 让人 0 挪去岗位 1，全链翻转成功。复杂度 `O(V·E)`——每个左点最多把全图边走一遍。更快的 Hopcroft-Karp 做到 O(E√V)，思想仍是增广路，成批地找而已。' },
      { t: 'h2', x: '为什么「找不到增广路」就是最大' },
      { t: 'p', x: 'Berge 定理：**匹配 M 最大 ⟺ 图中不存在关于 M 的增广路**。直觉一面：有增广路就能翻转 +1，显然没到头；另一面：若存在更大的匹配 M′，把 M 与 M′ 的边集做对称差，得到的每个连通块只能是交替路径或交替偶环，而 M′ 边数更多，必有一条 M′ 边多于 M 边的路径——它恰是 M 的增广路。所以匈牙利算法对每个左点都增广失败后停机，得到的就是全局最大匹配，不是局部凑合。' },
      { t: 'callout', kind: 'warn', title: '三个高频错误', x: [
        '**vis 清空的时机**：`vis` 在**每个左点开始增广前**清一次，管住的是「本轮」别绕圈；写到 for 循环外整场只清一次，后面的人被前人的标记堵死，答案偏小。',
        '**忘了逐个连通分量染色**：判定二分图时只从 0 号点染，够不着的分量没检查，孤立的三角形就漏网了。',
        '**匹配数组的方向**：`matchR` 以右侧为下标存「归谁」。翻转链条时只需改 `matchR[v] = u` 一处，方向存反就要同时维护两个数组，容易漏改。',
      ] },
      { t: 'quiz',
        q: '一个连通图含奇环，下列哪个结论一定成立？',
        opts: [
          '它的最大匹配为 0',
          '它不是二分图，染色法必然在某条边上报冲突',
          '它没有匹配的概念',
          '添加一个点就能变成二分图',
        ],
        answer: 1,
        explain: '奇环 ⟺ 非二分图，染色必撞车，这是本节的判定基石。但「匹配」概念对任意图都成立（一般图最大匹配用带花树算法，超纲不展开），奇环图照样可以有很大的匹配，选项 A、C 都是混淆。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: '二分图 ⟺ 无奇环 ⟺ 两色染色成功。最大匹配用匈牙利：为每个左点找增广路，「占用者挪位」递归到底，找到空位整链翻转 +1；找不到任何增广路即最大（Berge 定理）。vis 每轮一清，别写错位置。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '前两题把染色判定练熟（力扣没有裸匈牙利题），第三题去洛谷默写匈牙利模板。', x: [
        { id: 785, title: '判断二分图', slug: 'is-graph-bipartite', level: '中等',
          why: '染色判定的模板题：图已给成邻接表，专心练 BFS/DFS 两色染色。',
          hint: '未染色的点染 0，邻居染 1，交替下去；撞到「邻居已染且同色」立即返回 false。图可能不连通，逐个分量起跑。',
          insight: ['BFS 或 DFS 染色均为 O(V+E) 时间 O(V) 空间。染色冲突 ⟺ 抓到奇环，这就是本节判定基石的代码化。', '坑：只从 0 号点染一次会漏掉其它连通分量——本节「高频错误」第二条的原题。另一条路线是并查集：把每个点的所有邻居并成一集，若某点与其邻居同集则非二分图。'] },
        { id: 886, title: '可能的二分法', slug: 'possible-bipartition', level: '中等',
          why: '练「把关系翻译成图」：不喜欢 = 边，能分两组 ⟺ 这张图是二分图。',
          hint: '先把 dislikes 建成无向邻接表，然后就是 785 原题。建图这步才是本题的考点。',
          insight: '建图 + 染色 O(V+E)。坑：人的编号是 1~n，数组开 n+1 直接用原编号（图的表示一节的忠告）；没有任何 dislike 关系的人单独成分量，天然合法，别让循环漏掉他们。扩展域并查集（「朋友的敌人」域）是等价解法。' },
        { badge: '洛谷', title: '【模板】二分图最大匹配', url: 'https://www.luogu.com.cn/problem/P3386', level: '中等',
          why: '力扣没有裸匈牙利题，这道模板题专门检验 tryMatch 增广能不能默写对。',
          hint: '照本节 hungarian.cpp 的骨架：左点逐个增广，`vis` 在每个左点开始前清空。注意题目编号从 1 开始，且左右两侧点数不同。',
          insight: ['匈牙利算法 O(V×E)，n、m ≤ 500、e ≤ 5×10^4 稳过。matchR 按**右侧**点数开，方向别存反。', '坑：vis 清空写到循环外答案立刻偏小——本节「高频错误」第一条会在这题上直接现形；重边对算法无害，不必去重。'] },
      ] },
    ],
  },

  /* ------------------------------------------------------------
     10. 网络流入门：最大流最小割
     ------------------------------------------------------------ */
  'graph-flow': {
    lede: '把图当水管网：每条边有容量上限，问从源点到汇点每秒最多送多少水。反向边这颗「后悔药」是全部理解难点，吃透它，最大流就通了。',
    blocks: [
      { t: 'h2', x: '流网络的三个规矩' },
      { t: 'p', x: '**流网络**是一张有向图：**源点 s** 只出水，**汇点 t** 只收水，每条边标着**容量** c（每秒最多过多少）。一个合法的**流**要守两条规矩：每条边的流量 0 ≤ f ≤ c（不超载）；除 s、t 外每个点流入总量 = 流出总量（**守恒**，中间点不存水不漏水）。目标：让从 s 流出的总量最大——这就是**最大流**。它是排水系统、物流运力、带宽分配的抽象，也是一大批看似无关的组合问题的最终归宿。' },
      { t: 'h2', x: '增广路与那颗「后悔药」' },
      { t: 'p', x: '朴素想法：不断找一条 s 到 t 还有余量的路，沿路灌满，直到找不到。可惜**贪心会锁死**：先手选的路径可能占用了某条关键边，导致后续无路，而全局最优本该绕开它。修复的办法漂亮至极——**残量网络**：每条边灌了 f 之后，正向剩 c−f 的余量；同时凭空造一条容量 f 的**反向边**。反向边的含义是「允许把已灌进去的 f 退回来」：后来的增广路若走了反向边，等于对先前的决策说「这段我借走了，你改道」——先前的流被优雅地重新路由，而不是推倒重来。所以反向边就是**后悔药**：它让「贪心 + 可撤销」达到全局最优。动画里遇到走反向边的那一步，暂停想清楚它退掉了谁的哪一段：' },
      { t: 'viz', kind: 'graph-flow' },
      { t: 'h2', x: 'EK 算法：BFS 找增广路' },
      { t: 'p', x: 'Edmonds-Karp = 「在**残量网络**上反复 BFS 找增广路」：BFS 找到一条 s→t 的路（只走残量 > 0 的边），瓶颈 = 路上最小残量；沿路正向边扣掉瓶颈、反向边加上瓶颈；重复到 BFS 到不了 t。用 BFS（最少边数路径）而非 DFS 是有讲究的：可以证明增广轮数被限制在 O(VE)，总复杂度 `O(VE²)`——竞赛里更快的 Dinic 也只是把「一次找一条」升级成「分层批量找」，骨架一模一样。' },
      { t: 'code', lang: 'cpp', name: 'ek_maxflow.cpp（邻接矩阵残量版，可直接编译）', x: `#include <cstdio>
#include <cstring>
#include <queue>
using namespace std;
const int N = 6;
const long long INF = 1e18;

long long cap_[N][N];    // cap_[u][v]: u->v 的剩余容量（残量网络）
int pre[N];              // BFS 前驱，用于回溯增广路

long long bfsAug(int s, int t) {     // 找一条增广路，返回瓶颈（0=无路）
    memset(pre, -1, sizeof pre);
    pre[s] = s;
    queue<pair<int,long long>> q;    // {点, 到它为止的瓶颈}
    q.push({s, INF});
    while (!q.empty()) {
        auto [u, flow] = q.front(); q.pop();
        for (int v = 0; v < N; v++) {
            if (pre[v] != -1 || cap_[u][v] <= 0) continue;
            pre[v] = u;
            long long f = min(flow, cap_[u][v]);
            if (v == t) return f;    // 到汇点，瓶颈到手
            q.push({v, f});
        }
    }
    return 0;
}

int main() {
    int s = 0, t = 5;
    // 经典 6 点网络
    cap_[0][1] = 10; cap_[0][2] = 10;
    cap_[1][2] = 2;  cap_[1][3] = 4;  cap_[1][4] = 8;
    cap_[2][4] = 9;  cap_[4][3] = 6;
    cap_[3][5] = 10; cap_[4][5] = 10;

    long long maxflow = 0, f;
    while ((f = bfsAug(s, t)) > 0) {
        maxflow += f;
        for (int v = t; v != s; v = pre[v]) {   // 沿前驱回溯整条路
            int u = pre[v];
            cap_[u][v] -= f;         // 正向边扣余量
            cap_[v][u] += f;         // 反向边加“后悔额度”
        }
    }
    printf("最大流 = %lld\\n", maxflow);   // 输出 19
    return 0;
}` },
      { t: 'p', x: '矩阵版把「反向边」写成对称格子 `cap_[v][u]`，最能看清本质；大图（点多且稀疏）实战会换成链式前向星 + 成对存边（第 i 条边的反向边是 i^1），原理分毫不差。' },
      { t: 'h2', x: '最大流 = 最小割' },
      { t: 'p', x: '**割**：把点集切成含 s 的 S 与含 t 的 T 两半，割的容量 = 所有从 S 指向 T 的边容量之和——把这些边剪断，水就一滴到不了 t。任何流都得穿过任何割，所以**最大流 ≤ 最小割**恒成立。精彩的是等号：EK 停机时，在最终残量网络上从 s 还能到达的点组成 S，其余归 T；S→T 的每条原边必然被灌满（还有残量的话 BFS 就该穿过去了），这个割的容量恰好等于当前流量。于是**最大流 = 最小割**——算法停下来的那一刻，顺手把「拦截代价最小的封锁方案」也构造了出来。求最小割方案：最后再做一次 BFS 标出 S，跨越 S/T 的满流边就是答案。' },
      { t: 'h2', x: '应用一瞥：匹配归约到流' },
      { t: 'p', x: '上一节的二分图最大匹配可以整个塞进最大流：建超级源 s 连向左侧每点（容量 1），右侧每点连向超级汇 t（容量 1），原图的边从左指右（容量 1）。跑最大流，流量值 = 最大匹配数——容量 1 保证每人每岗只用一次，而流里的增广恰好对应匈牙利的「挪位置」。这类「造 s 和 t、用容量表达规则」的建模是网络流真正的考点：任务分配、项目选择（最小割）、棋盘覆盖，皆是此路。' },
      { t: 'callout', kind: 'danger', title: '忘建反向边 = 白学', x: '实现时最致命的错误是只扣正向余量、不加反向边（矩阵版漏写 `cap_[v][u] += f` 那一行）。算法照样跑、也能输出一个数，但没有「后悔」机制，先手贪心锁死后续路径，结果偏小——又是一个「多数样例能对、构造数据必挂」的隐形炸弹。' },
      { t: 'callout', kind: 'warn', title: '两个理解性提醒', x: [
        '**最大流看瓶颈，不是看源点出边之和**：源点出边总容量只是上界，真正限制流量的是全图最紧的那道「关口」（最小割）。',
        '**EK 与 DFS 找路的 Ford-Fulkerson 区别**：后者复杂度依赖容量数值，容量大时可能增广极多轮；EK 用 BFS 把轮数压到 O(VE)，与数值无关。入门用 EK，进阶学 Dinic。',
      ] },
      { t: 'quiz',
        q: '在残量网络上，一条增广路走了某条反向边，这在「原图」里意味着什么？',
        opts: [
          '原图这条边的流量继续增加',
          '之前灌进对应正向边的一部分流被退掉、改道重新路由',
          '出现了负的流量，网络非法',
          '算法出错，增广路不允许走反向边',
        ],
        answer: 1,
        explain: '反向边的容量正是已灌入的流量，走它就是消费「后悔额度」：原正向边的流被退掉一部分，由新路径接手运输。这完全合法，且正是最大流能修正早期错误贪心、达到全局最优的机制核心。' },
      { t: 'callout', kind: 'key', title: '本节要点', x: '流的两规矩：容量限制 + 中间点守恒。残量网络 = 正向剩余 + 反向后悔额度；EK = 反复 BFS 增广，停机时残量图 s 可达集就给出最小割，故最大流 = 最小割。建模套路：造源汇、用容量写规则——二分图匹配只是它的一个特例。' },
      { t: 'h2', x: '配套练习' },
      { t: 'exercises', intro: '力扣主站没有裸最大流题，两道洛谷题从入门到模板：先用矩阵版 EK 过小图，再换正经实现过大图。', x: [
        { badge: '洛谷', title: '[USACO4.2] 草地排水 Drainage Ditches', url: 'https://www.luogu.com.cn/problem/P2740', level: '中等',
          why: '最大流第一题：点和边都只有 200 规模，本节的矩阵版 EK 原样照搬就能过。',
          hint: '把本节 ek_maxflow.cpp 改成读输入即可，源点汇点编号按题面给定。唯一要动脑的地方：题目有重边。',
          insight: ['EK 算法 O(VE²)，这个规模毫无压力。矩阵版处理重边的正确姿势是 `cap_[u][v] += w` 累加容量，写成 `=` 覆盖就错——图的表示一节提过的老坑在网络流里换了张脸。', '过掉这题说明 EK 骨架和「正向扣、反向加」都写对了，可以去打真正的模板题。'] },
        { badge: '洛谷', title: '【模板】网络最大流', url: 'https://www.luogu.com.cn/problem/P3376', level: '困难',
          why: '标准模板题：n ≤ 200、m ≤ 5000，矩阵存不划算，逼你写链式前向星成对存边的正式版。',
          hint: '成对存边从编号 0 开始，第 i 条边的反向边就是 `i ^ 1`，残量直接存在边上。EK 可过；想一步到位就写 Dinic：BFS 分层，只沿层数 +1 的边 DFS 多路增广，配当前弧优化。',
          insight: ['Dinic 理论上界 O(V²E)，实际远快于此，是竞赛默认实现；EK O(VE²) 在此题也能过。', '三个坑：忘写反向边（本节「白学」警告的实战版）；答案会超 int，累计流量用 long long；写 Dinic 漏掉当前弧优化，特定数据会被卡到超时。过掉这题，最大流的实现关就算通了。'] },
      ] },
    ],
  },
};
